# Position Service
A simple position service for drones.

## Usage
1. Install the dependencies: `poetry install`
2. Run the tests: `poetry run pytest`
3. Run the service: `poetry run python -m position_service`
